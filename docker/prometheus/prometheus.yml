# Collector가 Remote Write로 메트릭을 push하므로 scrape 설정이 최소화됩니다.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # OTel Collector 자체 메트릭만 scrape (Collector 상태 모니터링용)
  - job_name: "otel-collector"
    static_configs:
      - targets: ["otel-collector:8888"]
