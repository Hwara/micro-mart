apiVersion: v1
kind: Service
metadata:
  labels:
    app: payment-service
  name: payment-service
  namespace: micro-mart
spec:
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: payment-service
status:
  loadBalancer: {}
