apiVersion: v1
kind: Service
metadata:
  labels:
    app: product-service
  name: product-service
  namespace: micro-mart
spec:
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app: product-service
status:
  loadBalancer: {}
